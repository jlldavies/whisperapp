[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    browser: browser-driven Playwright tests (require live app on ports 7860/7861)
asyncio_mode = auto
